<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net9.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
  </PropertyGroup>

  <!-- 
    Domain katmanı: HİÇBİR bağımlılığı yoktur.
    Sadece .NET BCL (Base Class Library) kullanır.
    Buraya NuGet paketi eklenmez (istisnai durumlar hariç).
  -->

</Project>
